<form method="post" action="{{ path('app_provincia_delete', {'id': provincia.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provincia.id) }}">
    <button class="btn">Delete</button>
</form>
