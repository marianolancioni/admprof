<form method="post" action="{{ path('app_circunscripcion_delete', {'id': circunscripcion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ circunscripcion.id) }}">
    <button class="btn">Delete</button>
</form>
