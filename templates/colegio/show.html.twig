    <table class="table shadow">
        <tbody>
            <tr>
                <th>Código:</th>
                <td>{{ colegio.id }}</td>
            </tr>
            <tr>
                <th>Colegio:</th>
                <td>{{ colegio.colegio }}</td>
            </tr>
            {#Para el campo Estado el FALSE or 0: Habilitado y el TRUE or 1: Deshabilitado#}
            <tr>
                <th>Estado:</th>
                <td>
                    {% if colegio.estado == true %}
                        1 - Deshabilitado
                    {% else %}
                        0 - Habilitado
                    {% endif %}
                </td>
            </tr>
            {#Para el campo Visible el FALSE or 0: No Visible y el TRUE or 1: Visible#}
            <tr>
                <th>Visible:</th>
                <td>
                    {% if colegio.visible == true %}
                        1 - Si
                    {% else %}
                        0 - No
                    {% endif %}
                </td>
            </tr>
        </tbody>
    </table>